/*
 * Button
 */

.btn
  border 0
  padding 1em 1.5em
  font-size 1rem
  font inherit
  border-radius 3px
  cursor pointer

.btn--primary
  background primaryColor
  color #fff

  &:hover
    background primaryColor + rgba(0, 0, 0, 0.25)
